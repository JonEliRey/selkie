# Agent start

Read [README.md](README.md), then follow [the operator guide](docs/operate-rule-lab.md) in order. For an orientation only, use the [visual architecture map](docs/architecture.md) or its [offline HTML/SVG version](docs/architecture.html); neither replaces the guide's command order or recovery authority. For a reusable qualification assignment, use [the task](docs/operator-tasks/run-rule-lab.md). Report exact evidence paths and values; stop on missing owner input or a host/runtime blocker. Keep tenant inputs and generated results in ignored local paths. Do not claim production approval or encode a vendor rule.
